First type this cmd - cd /var/www/pterodactyl
rm -rf *

Then this - cd /var/www/pterodactyl
curl -Lo panel.tar.gz https://github.com/reviactyl/panel/releases/latest/download/panel.tar.gz
tar -xzvf panel.tar.gz
chmod -R 755 storage/* bootstrap/cache/

Then this - COMPOSER_ALLOW_SUPERUSER=1 composer install --no-dev --optimize-autoloader

Then this - php artisan migrate --seed --force

Then this - chown -R www-data:www-data /var/www/pterodactyl/* 

And last this - sudo systemctl restart pteroq.service

Done Reviactyl theme installed 
------------------------------
